{
  "name": "json-nested-parser",
  "version": "1.0.0",
  "description": "Parser for nested JSON",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "json",
    "nested",
    "parser"
  ],
  "author": "butilities",
  "license": "MIT",
  "dependencies": {
    "is-valid-json": "^1.0.2",
    "traverse": "^0.6.6"
  },
  "devDependencies": {
    "jest": "^26.0.1"
  }
}
